import{Component,type ErrorInfo,type ReactNode}from'react';
export default class ErrorBoundary extends Component<{children:ReactNode},{failed:boolean}>{state={failed:false};static getDerivedStateFromError(){return{failed:true}}componentDidCatch(error:Error,info:ErrorInfo){if(import.meta.env.DEV)console.error(error,info)}render(){if(this.state.failed)return <main className="fatal"><div><h1>Đã xảy ra lỗi</h1><p>Vui lòng tải lại trang. Nếu lỗi vẫn tiếp diễn, hãy kiểm tra kết nối mạng hoặc liên hệ quản trị viên.</p><button onClick={()=>location.reload()}>Tải lại trang</button></div></main>;return this.props.children}}
